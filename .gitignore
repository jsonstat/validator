# Rust
target/
**/*.rs.bk

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Generated build inputs (regenerated, never hand-edited)
packages/ts/src/generated/
packages/wasm/pkg/
# NOTE: crates/validator/src/_vendored/ is intentionally COMMITTED (not gitignored). It is the
# self-contained snapshot that `cargo publish` packs and the verification build reads via
# include_str!. build.rs keeps it in sync with ../../rules-manifest.json and
# ../../schemas/vendored/*.json during local dev; a parity test guards against drift.

# OS / editors
.DS_Store
